name: Docker API build check

# Smoke-build the TS Express image on every PR that touches the API source,
# the Dockerfile, or the entrypoint script. Catches Dockerfile / package-lock /
# tsconfig drift before it lands in dev and breaks the Fly deploy in HEL-83.
#
# Does not push the image — that's the deploy workflow's job (per env).

on:
  pull_request:
    paths:
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "docker/api/**"
      - ".dockerignore"
      - ".github/workflows/docker-api-build-check.yml"
  push:
    branches: [dev]
    paths:
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "docker/api/**"
      - ".dockerignore"
      - ".github/workflows/docker-api-build-check.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docker-api-build-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build autoflow-api image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - name: Build image (no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/api/Dockerfile
          push: false
          load: true
          tags: autoflow-api:check
          # HEL-234: rev the cache scope to force a cold build. The previous
          # `autoflow-api` scope was failing on every PR that touched src/**
          # in 8-15s — too fast for a real build, consistent with a corrupted
          # cache layer. Bumping to v2 invalidates everything.
          cache-from: type=gha,scope=autoflow-api-v2
          cache-to: type=gha,mode=max,scope=autoflow-api-v2
          build-args: |
            BUILD_SHA=${{ github.sha }}
            BUILD_DATE=${{ github.event.pull_request.updated_at || github.event.head_commit.timestamp }}

      - name: Inspect image size
        run: |
          set -euo pipefail
          size_bytes="$(docker image inspect autoflow-api:check --format='{{.Size}}')"
          size_mb=$(( size_bytes / 1024 / 1024 ))
          echo "Image size: ${size_mb} MB"
          echo "image_size_mb=${size_mb}" >> "$GITHUB_OUTPUT"

          # Soft cap — warn at 1000 MB, fail at 1100 MB. Bumped from 600/800 in
          # late May 2026 once @anthropic-ai/claude-agent-sdk started shipping a
          # ~225 MB Linux native binary per platform variant (linux-x64 +
          # linux-x64-musl both land on a glibc runtime because the libc
          # constraint isn't recorded in lockfile entries). Trimming the SDK
          # binaries upstream is fragile, so we accept the larger floor here.
          if [ "$size_mb" -gt 1100 ]; then
            echo "::error::Image size ${size_mb} MB exceeds the 1100 MB hard cap. Check for dev deps slipping into the runtime stage."
            exit 1
          fi
          if [ "$size_mb" -gt 1000 ]; then
            echo "::warning::Image size ${size_mb} MB is above the 1000 MB soft target."
          fi

      - name: Verify entrypoint + node binary
        run: |
          set -euo pipefail

          # Sanity: the entrypoint script is executable and the runtime image
          # has node + infisical installed where we expect.
          docker run --rm --entrypoint sh autoflow-api:check -c "ls -la /usr/local/bin/autoflow-api-entrypoint && which node && which infisical"

      - name: Verify HEALTHCHECK metadata
        run: |
          set -euo pipefail
          docker image inspect autoflow-api:check --format='{{json .Config.Healthcheck}}'

      - name: Summarize
        run: |
          {
            echo "## Docker API build check"
            echo
            echo "- Image: \`autoflow-api:check\`"
            echo "- BUILD_SHA: ${{ github.sha }}"
            echo "- Status: built successfully"
          } >> "$GITHUB_STEP_SUMMARY"
